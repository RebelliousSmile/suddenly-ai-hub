# Double review after implementation

- After every implementation, run both reviews in parallel:
  - `aidd:05:review_functional`
  - `aidd:05:review_code`
- Fix all issues found by both reviews before proceeding
- Do not commit until both reviews pass clean
