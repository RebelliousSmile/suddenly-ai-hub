[security]
# Configuration de sécurité pour le scraping
# ⚠️ NE JAMAIS COMMITTER CES CREDENTIALS !

# jdRoll credentials (à configurer manuellement)
# OPTION 1: Utiliser login/mdp (recommandé pour débuter)
JDROLL_USERNAME=tnntwister
JDROLL_PASSWORD=hermes123!

# OPTION 2: Utiliser des cookies sauvegardés (plus sécurisé)
# Lancer une fois avec --login, puis utiliser --cookies

[scraping]
# Paramètres de scraping
max_forums=2
max_topics_per_forum=20
delay_between_requests=3

[output]
# Dossiers de sortie
data_dir=data/raw
clean_dir=data/clean
eval_dir=data/eval
